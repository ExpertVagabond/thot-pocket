[package]
name = "thot-pocket-demo"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
thot-pocket = { path = ".." }
wasm-bindgen = "0.2"
glam = "0.29"
getrandom = { version = "0.2", features = ["js"] }

[profile.release]
opt-level = "s"
lto = true
